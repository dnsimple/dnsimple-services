1. Open your project in the Upsun console.
1. Go to **Settings** > **Domains** and copy the CNAME target shown for your project.
1. Add `www.yourdomain.com` as a custom domain in the Upsun console if it is not already configured.

If you want the apex domain to redirect to `www`, handle that separately using
the DNSimple's redirect feature.
